server:
  port: 8080

info:
  artifactId: "${project.name}"
  version: "${project.version}"

spring:
  application:
    name: Totonero-Service
  datasource:
    url: jdbc:mysql://localhost:3306/financial?createDatabaseIfNotExist=true&useSSL=false&useTimezone=true&serverTimezone=UTC
    username: root
    password: '12345678'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: none

management:
  endpoints:
    web:
      exposure:
        include: "*"

jwt:
  expiration: 898989875
  secret: QnJlbm9Mb3Blc0VSb2RpbmVp